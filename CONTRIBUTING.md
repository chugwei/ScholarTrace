# 贡献指南

## 开发原则

- `main` 必须保持可安装、可运行、可测试。
- 功能按版本路线逐步落地，未通过当前版本门禁时不提前发布后续能力。
- 代码改动应同步包含相应测试、文档和追溯记录。
- LLM 输出只作为候选内容；正式科研产物必须通过结构校验、来源验证和人工审批。
- 不提交密钥、凭据、私有文献、真实私有数据、数据库、模型权重或大型运行产物。
- 不伪造论文、DOI、引文、实验指标、图表、部署结果或真实场景证据。

## 本地验证

```bash
uv sync --all-groups
uv run python scripts/check.py
```

提交前请确认 `git diff --check`、Ruff、pytest 和相关集成测试通过，并检查暂存区不包含秘密或运行产物。

## Git 约定

- 使用清晰的 Conventional Commits。
- 每个版本使用独立功能分支，验收后合并到 `main`。
- 不重写已共享历史，不提交无法验证的完成声明。
